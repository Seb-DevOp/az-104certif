node_modules/
web/dist/
web/tsconfig.tsbuildinfo

# Local production preview only (`npm run serve` copies web/dist here).
# Anchored with a leading slash: web/public/ is tracked application data.
/public/

# The source PDF stays out: it is 17 MB, it is not needed to build the image, and
# `npm run data` only needs it when the question bank is regenerated. The generated bank
# under web/public/ IS tracked, so a plain clone builds and deploys as-is.
AZ-104_dump.pdf
tools/.dump.txt

.env
.env.local
*.log
tools/_c.mjs
