# gcloud uses THIS file, not .gitignore, to decide what to upload.
# Leading slashes matter: an unanchored pattern matches at any depth, which would
# silently drop web/public/data/ — the question bank the build needs.
.git/
**/node_modules/
**/dist/

/public/
/tools/
AZ-104_dump.pdf
